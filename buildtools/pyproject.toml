[project]
name = "gte2-buildtools"
version = "1.1.1"
description = "Build tools for GTE2 Modpack"
requires-python = ">=3.14"
dependencies = [
    "json5>=0.12.1",
    "requests>=2.33.0",
]

[tool.uv]
package = false
